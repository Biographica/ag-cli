---
phase: 02.1-integration-fixes
verified: 2026-02-26T10:30:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
gaps: []
human_verification: []
---

# Phase 2.1: Integration Fixes Verification Report

**Phase Goal:** All cross-phase wiring between Phase 1 and Phase 2 works correctly — no spurious warnings, no standalone species maps diverging from the YAML registry, no stale CLI branding in user-facing output
**Verified:** 2026-02-26T10:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                             | Status     | Evidence                                                                                       |
| --- | ------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------- |
| 1   | `load_expression()` called with no species argument does NOT emit a "not in registry" warning    | VERIFIED   | Default at line 119: `species: str = "Arabidopsis thaliana"` — space form matches YAML key    |
| 2   | `parity.mygene_lookup` resolves species via `_species.py` backed by `species_registry.yaml`      | VERIFIED   | `from ct.tools._species import resolve_species_taxon` at line 15; called at line 79/91        |
| 3   | A new species added to `species_registry.yaml` is automatically recognized by `mygene_lookup`    | VERIFIED   | `_normalize_mygene_species` delegates unknown species to `resolve_species_taxon`; proven by `test_mygene_lookup_yaml_backed_species` |
| 4   | `ag data pull plantexp` manual-download output says `ag config set` not `ct config set`          | VERIFIED   | Line 167 of `downloader.py`: `ag config set data.{name} {dest}`; no `ct config set` remains  |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                          | Expected                                          | Status     | Details                                                                                                      |
| --------------------------------- | ------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------ |
| `src/ct/tools/plant_data.py`      | Fixed default species parameter                   | VERIFIED   | Line 119: `species: str = "Arabidopsis thaliana"`. Line 107 descriptor updated. No stale underscore form.   |
| `src/ct/tools/parity.py`          | YAML-backed species resolution for MyGene.info    | VERIFIED   | `_MYGENE_SPECIES_MAP` deleted. `resolve_species_taxon` imported and called. Slim reference + parasite maps retained. |
| `src/ct/data/downloader.py`       | Correct CLI branding in manual-download path      | VERIFIED   | Line 167 outputs `ag config set`. Grep for `ct config set` returns zero matches.                             |
| `tests/test_plant_data.py`        | Test for default species no-warning behavior      | VERIFIED   | `test_load_expression_default_species_no_warning` at line 207 passes.                                       |
| `tests/test_parity_tools.py`      | Tests for YAML-backed species resolution          | VERIFIED   | `test_mygene_lookup_plant_species_via_registry` (line 56) and `test_mygene_lookup_yaml_backed_species` (line 75) both pass. |
| `tests/test_downloader.py`        | Test for correct branding + plantexp in expected datasets | VERIFIED | `test_plantexp_manual_download_branding` (line 158) passes. `test_expected_datasets_present` includes `plantexp`. |

### Key Link Verification

| From                          | To                            | Via                                              | Status   | Details                                                                                           |
| ----------------------------- | ----------------------------- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------- |
| `src/ct/tools/parity.py`      | `src/ct/tools/_species.py`    | `from ct.tools._species import resolve_species_taxon` | WIRED | Import at line 15 confirmed. Function called at lines 79 and 91 in `_normalize_mygene_species`.  |
| `src/ct/tools/plant_data.py`  | `src/ct/tools/_species.py`    | default species string matches registry key format | WIRED  | Default `"Arabidopsis thaliana"` normalises to key `"arabidopsis thaliana"` in `_build_lookup()`. Runtime check confirms default is `('Arabidopsis thaliana', 'plantexp', '')`. |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                  | Status    | Evidence                                                                                  |
| ----------- | ----------- | ---------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------- |
| DATA-03     | 02.1-01     | Organism validation middleware validates species before returning results     | SATISFIED | `@validate_species` decorator wired. Default species now resolves correctly — no spurious warning on `load_expression()`. Test `test_load_expression_default_species_no_warning` passes. |
| DATA-04     | 02.1-01     | Species registry — central registry with taxon ID, common name, genome build | SATISFIED | `resolve_species_taxon` from `_species.py` (backed by `species_registry.yaml`) is now called by `parity.py`. Rice resolves to taxon `4530` at runtime. |
| FOUN-03     | 02.1-01     | Species-agnostic architecture — species passed as parameter, not hardcoded   | SATISFIED | Monolithic `_MYGENE_SPECIES_MAP` (50-line hardcoded plant species dict) deleted from `parity.py`. All plant species now resolved via YAML registry. No hardcoded species remain in modified files. |
| FOUN-04     | 02.1-01     | CLI and pyproject.toml rebranded from celltype-cli to ag-cli                 | SATISFIED | `downloader.py` line 167 now reads `ag config set` — last stale `ct` branding reference in user-facing output is removed. |

All four requirement IDs declared in the PLAN frontmatter are accounted for and satisfied.

**Orphaned requirements check:** REQUIREMENTS.md traceability table maps DATA-03, DATA-04 to Phase 2 and FOUN-03, FOUN-04 to Phase 1. Phase 2.1 is an unregistered gap-closure phase in REQUIREMENTS.md (the table does not have a Phase 2.1 row). This is expected — the PLAN frontmatter declares `gap_closure: true` and the ROADMAP correctly documents these as gap-closure requirements. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| None | — | — | — | — |

No TODOs, FIXMEs, placeholder returns, or empty handler stubs found in any of the six modified files.

### Human Verification Required

None. All behaviors are verifiable programmatically through the test suite and runtime spot-checks.

### Test Results

**Phase-scoped tests (49 tests):** 49 passed, 0 failed — confirmed against `tests/test_plant_data.py`, `tests/test_parity_tools.py`, `tests/test_downloader.py`.

**Full suite (886 non-skipped tests):** 28 failures in files unrelated to this phase (`test_chemistry_new.py` — rdkit not installed, `test_cli.py` — TraceLogger undefined, `test_omics.py`, `test_sandbox.py`, `test_shell.py`, `test_terminal.py`, `test_mention_completer.py`). These are confirmed pre-existing failures. No regressions introduced by this phase.

**Phase commits verified in git history:**
- `4437636` — test(02.1-01): add tests for three integration fixes
- `4499e01` — fix(02.1-01): apply three integration fixes from milestone audit

### Runtime Spot-Checks

All three spot-checks from the PLAN's `<verification>` section pass:

1. `load_expression.__wrapped__.__defaults__` returns `('Arabidopsis thaliana', 'plantexp', '')` — correct space form confirmed.
2. `_normalize_mygene_species("rice")` returns `"4530"` — YAML registry path confirmed end-to-end.
3. `grep "ct config set" src/ct/data/downloader.py` returns zero matches — stale branding fully removed.
4. `grep "ag config set" src/ct/data/downloader.py` returns line 167 — correct branding present.

---

_Verified: 2026-02-26T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
