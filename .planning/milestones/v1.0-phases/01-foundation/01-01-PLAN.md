---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ct/agent/system_prompt.py
  - src/ct/agent/knowledge.py
  - src/ct/tools/__init__.py
  - src/ct/agent/runner.py
  - src/ct/cli.py
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "Agent responds to plant biology questions with plant-focused reasoning, no oncology or drug discovery framing"
    - "Running `ag tool list` shows only plant-relevant and general-purpose tools; pharma-category tools are absent"
    - "Agent never attempts to call pharma-category tools during a plant biology query (tools are invisible at MCP layer)"
    - "System prompt refers to the agent as 'Harvest' and describes plant biology/ag biotech domain expertise"
    - "System prompt contains no references to drug discovery, clinical trials, IC50, or pharma workflows"
  artifacts:
    - path: "src/ct/agent/system_prompt.py"
      provides: "Plant science agent identity and operating instructions"
      contains: "AGENT_NAME = \"Harvest\""
    - path: "src/ct/agent/knowledge.py"
      provides: "Plant biology domain knowledge primer"
      contains: "KNOWLEDGE_PRIMER"
    - path: "src/ct/tools/__init__.py"
      provides: "PLANT_SCIENCE_CATEGORIES allowlist constant"
      contains: "PLANT_SCIENCE_CATEGORIES"
    - path: "src/ct/agent/runner.py"
      provides: "Exclude-set computation from allowlist"
      contains: "PLANT_SCIENCE_CATEGORIES"
    - path: "src/ct/cli.py"
      provides: "Filtered tool list command"
      contains: "PLANT_SCIENCE_CATEGORIES"
  key_links:
    - from: "src/ct/tools/__init__.py"
      to: "src/ct/agent/runner.py"
      via: "PLANT_SCIENCE_CATEGORIES import for exclude-set computation"
      pattern: "from ct\\.tools import.*PLANT_SCIENCE_CATEGORIES"
    - from: "src/ct/agent/runner.py"
      to: "src/ct/agent/mcp_server.py"
      via: "exclude_categories parameter passed to create_ct_mcp_server()"
      pattern: "exclude_categories=exclude_cats"
    - from: "src/ct/tools/__init__.py"
      to: "src/ct/cli.py"
      via: "PLANT_SCIENCE_CATEGORIES import for tool list filtering"
      pattern: "PLANT_SCIENCE_CATEGORIES"
---

<objective>
Replace the oncology/pharma system prompt with a plant science identity and implement runtime tool category filtering so the agent operates exclusively in the plant biology domain.

Purpose: This is the foundational domain swap — after this plan, the agent reasons as a plant science expert ("Harvest") and can only see plant-relevant tools. Without this, the agent still thinks it is a drug discovery assistant and surfaces 190+ pharma tools.

Output: Updated system_prompt.py (plant science _IDENTITY), knowledge.py (plant biology KNOWLEDGE_PRIMER), tools/__init__.py (PLANT_SCIENCE_CATEGORIES constant + list_tools_table filtering), runner.py (exclude-set wiring), cli.py (tool list uses allowlist).
</objective>

<execution_context>
@/Users/dominichall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dominichall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

@src/ct/agent/system_prompt.py
@src/ct/agent/knowledge.py
@src/ct/tools/__init__.py
@src/ct/agent/runner.py
@src/ct/agent/mcp_server.py
@src/ct/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace system prompt with plant science identity and knowledge primer</name>
  <files>src/ct/agent/system_prompt.py, src/ct/agent/knowledge.py</files>
  <action>
**system_prompt.py changes:**

1. Add module-level constant: `AGENT_NAME = "Harvest"` — this is the single configurable value for the agent's name (user decision: name may change later, keep in one place).

2. Replace `_IDENTITY` with a plant science identity per CONTEXT.md locked decisions:
   - Expert collaborator tone — assumes domain knowledge, uses technical language, focuses on evidence and reasoning
   - Clean slate identity — NO acknowledgment of pharma/drug discovery heritage; purpose-built for plant science
   - Ag biotech framing lens but deeply plant biology: genomics, expression, regulatory networks, orthologs, trait development, gene editing, breeding
   - Do NOT include shortlisting metric language (novelty, efficacy, pleiotropic risk, editability) — the agent is an expert plant biology substrate, not pre-optimized for any workflow
   - Reference `AGENT_NAME` constant (f-string) so name is configurable
   - Include operating mode section (agentic loop, tool use, self-correction, species inference from context)
   - Use the _IDENTITY template from 01-RESEARCH.md Pattern 4 as a starting point

3. Update the tool catalog hint section in `build_system_prompt()`:
   - Remove all pharma-category tool references (chemistry.descriptors, clinical.indication_map, safety.classify, viability.dose_response, etc.)
   - Replace with plant-relevant tool orientation: genomics, network/PPI, literature, expression, data exploration, DNA tools, Python sandbox
   - Keep the hint structure (it helps Claude use tools effectively) but make all examples plant biology

4. Update `_SYNTHESIS_INSTRUCTIONS`:
   - Replace any pharma-specific synthesis guidance (IC50, clinical trials, drug mechanisms) with plant biology outputs (gene function, expression patterns, ortholog evidence, trait associations, gene editing considerations)
   - Keep the general synthesis structure (be rigorous, cite evidence, acknowledge gaps)

5. Leave `SYNTHESIZER_PRIMER` grounding rules as-is (they are domain-agnostic per research findings).

**knowledge.py changes:**

6. Replace `KNOWLEDGE_PRIMER` entirely with a plant biology domain knowledge primer covering:
   - Plant genomics fundamentals (gene families, polyploidy, synteny, genome duplication events)
   - Expression biology (tissue-specific expression, developmental stages, abiotic/biotic stress responses)
   - Regulatory networks (transcription factors, hormone signaling pathways: auxin, gibberellin, ethylene, ABA, cytokinin, brassinosteroid)
   - Ortholog and comparative genomics (model species: Arabidopsis; major crops: rice, maize, wheat, soybean, tomato; phylogenetic distance considerations)
   - Gene editing context (CRISPR-Cas9 in plants, PAM site considerations for plant genomes, delivery methods: Agrobacterium, biolistics, protoplast; off-target in polyploid genomes)
   - Trait development (QTL mapping, GWAS, marker-assisted selection, transgenic vs cisgenic vs gene-edited)
   - Key databases and resources the agent should know about (TAIR, Phytozome, Gramene, PlantExp, Ensembl Plants, STRING plant organisms)
   - Keep it concise but substantive — this is the domain grounding that makes the agent a knowledgeable plant scientist, not a generic LLM

Do NOT reference any pharma concepts (drug targets, IC50, clinical phases, combination therapy, safety signals, etc.) anywhere in the new primer.
  </action>
  <verify>
    <automated>cd /Users/dominichall/projects/biographica/ag-cli && python -c "from ct.agent.system_prompt import build_system_prompt, AGENT_NAME; prompt = build_system_prompt([]); assert AGENT_NAME == 'Harvest'; assert 'Harvest' in prompt; assert 'drug' not in prompt.lower() or 'drug' not in prompt.split('Harvest')[0].lower(); assert 'oncology' not in prompt.lower(); assert 'IC50' not in prompt; assert 'plant' in prompt.lower(); assert 'genomics' in prompt.lower(); print('PASS: system prompt is plant science domain')"</automated>
    <manual>Read the generated system prompt and verify it reads as a credible plant science expert — not a repurposed pharma bot</manual>
  </verify>
  <done>System prompt presents as "Harvest", an expert plant biology/ag biotech research agent. No pharma/oncology/drug discovery language anywhere in _IDENTITY, KNOWLEDGE_PRIMER, tool catalog hints, or synthesis instructions. AGENT_NAME is a single module-level constant.</done>
</task>

<task type="auto">
  <name>Task 2: Implement category allowlist filtering for plant science tools</name>
  <files>src/ct/tools/__init__.py, src/ct/agent/runner.py, src/ct/cli.py</files>
  <action>
**src/ct/tools/__init__.py changes:**

1. Add `PLANT_SCIENCE_CATEGORIES` as a module-level `frozenset` constant. Per research Open Question #2, use the conservative allowlist (exclude borderline categories like repurposing, cellxgene, clue that are human/compound-centric):
   ```python
   PLANT_SCIENCE_CATEGORIES = frozenset({
       "genomics", "network", "literature", "data_api", "protein",
       "omics", "statistics", "dna", "code", "files", "shell",
       "ops", "claude", "expression", "singlecell", "notification",
       "remote_data", "experiment", "parity",
   })
   ```

2. Add `include_categories` parameter to `list_tools_table()` method on `ToolRegistry`:
   ```python
   def list_tools_table(self, include_categories: frozenset | None = None) -> Table:
   ```
   When `include_categories` is provided, skip tools whose `category` is not in the set. Update the table title to "ag Tools" (from "ct Tools" or whatever it currently is).

**src/ct/agent/runner.py changes:**

3. In `_run_async()` (or wherever `create_ct_mcp_server()` is called), compute the exclude set from the allowlist:
   ```python
   from ct.tools import registry, EXPERIMENTAL_CATEGORIES, PLANT_SCIENCE_CATEGORIES
   all_categories = set(registry.categories())
   exclude_cats = (all_categories - PLANT_SCIENCE_CATEGORIES) | set(EXPERIMENTAL_CATEGORIES)
   ```
   Pass `exclude_categories=exclude_cats` to `create_ct_mcp_server()`. The MCP server already accepts this parameter and filters tools at registration time (confirmed in research), so pharma tools become genuinely invisible to the agent (hard invisible — per Claude's discretion, this is the cleanest approach).

**src/ct/cli.py changes:**

4. In the `tool_list` command function, import `PLANT_SCIENCE_CATEGORIES` and pass it to `list_tools_table()`:
   ```python
   from ct.tools import registry, ensure_loaded, tool_load_errors, PLANT_SCIENCE_CATEGORIES
   console.print(registry.list_tools_table(include_categories=PLANT_SCIENCE_CATEGORIES))
   ```

**IMPORTANT:** Do NOT modify `mcp_server.py` — it already supports `exclude_categories`. The wiring happens in `runner.py`.

**Category audit note:** If during implementation you discover additional pharma-specific categories not in the exclude list (e.g., `target`, `translational`, `regulatory`, `intel`, `report`, `imaging` per research findings), they will be automatically excluded because they are NOT in PLANT_SCIENCE_CATEGORIES (allowlist approach means anything not explicitly allowed is hidden).
  </action>
  <verify>
    <automated>cd /Users/dominichall/projects/biographica/ag-cli && python -c "
from ct.tools import registry, ensure_loaded, PLANT_SCIENCE_CATEGORIES
ensure_loaded()
all_cats = set(registry.categories())
excluded = all_cats - PLANT_SCIENCE_CATEGORIES
print(f'Total categories: {len(all_cats)}')
print(f'Allowed: {len(PLANT_SCIENCE_CATEGORIES & all_cats)}')
print(f'Excluded: {len(excluded)}')
print(f'Excluded list: {sorted(excluded)}')
pharma_cats = {'chemistry', 'clinical', 'safety', 'cro', 'viability', 'combination', 'structure', 'biomarker', 'pk'}
assert pharma_cats.issubset(excluded), f'Pharma categories not excluded: {pharma_cats - excluded}'
print('PASS: pharma categories excluded by allowlist')
"</automated>
    <manual>Run `ag tool list` after Plan 02 rebrand and verify no pharma tools appear</manual>
  </verify>
  <done>PLANT_SCIENCE_CATEGORIES frozenset is defined in tools/__init__.py. Runner computes exclude set from allowlist and passes to MCP server. `tool list` CLI command filters by allowlist. Pharma-specific categories (chemistry, clinical, safety, CRO, viability, combination, structure, biomarker, PK, and all others not in the allowlist) are hidden from both the agent and the tool list.</done>
</task>

</tasks>

<verification>
1. `python -c "from ct.agent.system_prompt import build_system_prompt, AGENT_NAME; assert AGENT_NAME == 'Harvest'"` — agent name constant exists
2. `python -c "from ct.agent.system_prompt import build_system_prompt; p = build_system_prompt([]); assert 'oncology' not in p.lower(); assert 'drug discovery' not in p.lower()"` — no pharma language in prompt
3. `python -c "from ct.tools import PLANT_SCIENCE_CATEGORIES; assert 'genomics' in PLANT_SCIENCE_CATEGORIES; assert 'chemistry' not in PLANT_SCIENCE_CATEGORIES"` — allowlist correct
4. `pytest tests/ -x -q --timeout=30` — existing tests still pass (may need adjustments for changed defaults)
</verification>

<success_criteria>
- AGENT_NAME = "Harvest" is a single module-level constant in system_prompt.py
- _IDENTITY describes a plant science research agent with no pharma heritage
- KNOWLEDGE_PRIMER covers plant biology domain knowledge (no oncology)
- Tool catalog hints reference only plant-relevant tools
- PLANT_SCIENCE_CATEGORIES frozenset defines the allowlist
- Runner passes computed exclude set to MCP server
- `tool list` command filters by allowlist
- No pharma-category tool is visible to the agent or in tool listings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
