---
phase: 02.2-integration-fixes-ii
verified: 2026-02-26T11:26:09Z
status: passed
score: 2/2 must-haves verified
re_verification: false
---

# Phase 02.2: Integration Fixes II — Verification Report

**Phase Goal:** CLI routing correctly dispatches `ag species list` to the species subcommand, and clean installs succeed without missing PyYAML dependency
**Verified:** 2026-02-26T11:26:09Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `ag species list` via the installed CLI entry point dispatches to the species subcommand and returns the species registry — not a natural-language query | VERIFIED | `"species"` is present in the `passthrough` set in `entry()` at `src/ct/cli.py:1467`; `species_app` is registered with `app.add_typer(species_app, name="species")` at line 297; `@species_app.command("list")` at line 300; `test_entry_preserves_species_subcommand` passes and asserts `args == ["species", "list"]` (not `["run", "species", "list"]`) |
| 2 | A clean `pip install` in a fresh virtualenv succeeds and `import yaml` works in `_species.py` and `manifest.py` because PyYAML is declared as a runtime dependency | VERIFIED | `"pyyaml>=6.0"` present in `[project] dependencies` in `pyproject.toml:30`; `import yaml` confirmed in `src/ct/tools/_species.py:40` and `src/ct/data/manifest.py:40`; `test_pyyaml_declared_as_dependency` passes |

**Score:** 2/2 truths verified

### Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Status |
|----------|----------|--------|-------------|-------|--------|
| `src/ct/cli.py` | entry() passthrough set includes `"species"` | Yes | Yes — `"species"` at line 1467 in passthrough set; `entry()` routes argv unchanged when first arg is in set | Yes — passthrough directly gates the `app(args=argv)` call at line 1482 | VERIFIED |
| `pyproject.toml` | PyYAML declared as runtime dependency | Yes | Yes — `"pyyaml>=6.0"` at line 30 in `[project] dependencies` block | Yes — both `_species.py` and `manifest.py` do `import yaml` at runtime | VERIFIED |
| `tests/test_cli.py` | Test `test_entry_preserves_species_subcommand` exists | Yes | Yes — full monkeypatch test at line 115; patches `ct.cli.app`, sets `sys.argv = ["ag", "species", "list"]`, asserts `args == ["species", "list"]`; passes | Yes — runs in pytest suite; 2 passed in 0.21s | VERIFIED |
| `tests/test_manifest.py` | Test `test_pyyaml_declared_as_dependency` exists | Yes | Yes — reads pyproject.toml as text at line 210, asserts `"pyyaml" in contents.lower()`; passes | Yes — runs in pytest suite; all 14 tests in file pass | VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/ct/cli.py entry()` | `species_app` (line 297) | passthrough set allows argv to reach Typer dispatch | WIRED | `"species"` at line 1467 in passthrough set; `if not argv or argv[0] not in passthrough: argv = ["run", *argv]` at line 1479 — species bypasses this rewrite; `app.add_typer(species_app, name="species")` at line 297 ensures Typer routes `["species", "list"]` to the list command |
| `pyproject.toml [project] dependencies` | `src/ct/tools/_species.py` + `src/ct/data/manifest.py` | pyyaml declared ensures yaml importable on clean install | WIRED | `"pyyaml>=6.0"` confirmed at `pyproject.toml:30`; `import yaml` at `_species.py:40` and `manifest.py:40` — both are lazy imports confirming runtime usage |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DATA-01 | 02.2-01-PLAN.md | Agent can explore and analyse data from a local project folder using the Python sandbox | SATISFIED | pyyaml dependency declaration removes a silent install failure that would prevent `manifest.py` (the manifest loader used by data tools) from running; manifest.py imports yaml at runtime |
| DATA-02 | 02.2-01-PLAN.md | Data manifest pattern — each data folder has a manifest describing available datasets, species, schema, and provenance | SATISFIED | pyyaml fix ensures manifest.py can `import yaml` to load YAML manifests; `test_pyyaml_declared_as_dependency` guards regression |
| DATA-04 | 02.2-01-PLAN.md | Species registry — central registry of supported species with metadata | SATISFIED | `ag species list` now routes correctly to the species subcommand (which reads the YAML species registry) instead of falling through to the NL query handler; `test_entry_preserves_species_subcommand` guards regression |

No orphaned requirements for this phase. REQUIREMENTS.md traceability table maps DATA-01, DATA-02, and DATA-04 to "Phase 2, 2.2" with status "Complete (integration fix pending)" — all three are now satisfied by this phase.

### Anti-Patterns Found

No anti-patterns detected in any modified file (`src/ct/cli.py`, `pyproject.toml`, `tests/test_cli.py`, `tests/test_manifest.py`). No TODO, FIXME, placeholder, stub, or empty-implementation patterns found.

### Pre-existing Test Failures (Out of Scope)

8 tests in `tests/test_cli.py` fail due to `ct.agent.trace` module not existing (`ModuleNotFoundError`) and `TraceLogger` being undefined (`NameError`). These failures:
- Existed before Phase 02.2 (confirmed by executor via `git stash` verification)
- Are documented in `.planning/phases/02.2-integration-fixes-ii/deferred-items.md`
- Are unrelated to CLI routing or PyYAML — they concern a separate missing module
- Do not affect the 2 new tests or the 27 other passing tests in the file

The rdkit failure in `tests/test_chemistry_new.py` is similarly pre-existing (optional `rdkit` dependency not installed in the current environment).

### Human Verification Required

None. Both fixes are fully verifiable programmatically:
- CLI routing logic is a deterministic set membership check — grep and test execution confirm it
- PyYAML dependency is a static string in a TOML file — grep and test execution confirm it

## Gaps Summary

No gaps. All must-haves are satisfied:

1. `"species"` is in the `entry()` passthrough set at `src/ct/cli.py:1467`
2. `species_app` is registered with Typer at line 297 and has a `list` command at line 300
3. The routing chain is fully wired: `entry()` → passthrough check → `app(args=["species","list"])` → `species_app.list`
4. `"pyyaml>=6.0"` is in `[project] dependencies` at `pyproject.toml:30`
5. Both runtime consumers (`_species.py:40`, `manifest.py:40`) use lazy `import yaml`
6. `test_entry_preserves_species_subcommand` passes — asserts args are NOT prefixed with `"run"`
7. `test_pyyaml_declared_as_dependency` passes — asserts pyyaml string in pyproject.toml
8. All 14 tests in `tests/test_manifest.py` pass; 27 of 35 tests in `tests/test_cli.py` pass (8 pre-existing failures unrelated to this phase)

---

_Verified: 2026-02-26T11:26:09Z_
_Verifier: Claude (gsd-verifier)_
