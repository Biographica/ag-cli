---
phase: 02.2-integration-fixes-ii
plan: "01"
subsystem: cli
tags: [typer, pyyaml, cli-routing, entry-point, pyproject]

requires:
  - phase: 02.1-integration-fixes
    provides: "Integration fixes for species registry, expression loading, parity.py drift"
provides:
  - "ag species list routes correctly through entry() passthrough set"
  - "pyyaml>=6.0 declared as runtime dependency in pyproject.toml"
  - "Tests: test_entry_preserves_species_subcommand, test_pyyaml_declared_as_dependency"
affects: [future CLI subcommand additions, clean install verification]

tech-stack:
  added: ["pyyaml>=6.0 (runtime dependency declared)"]
  patterns:
    - "entry() passthrough set — all Typer subcommands must be listed here for correct routing"

key-files:
  created: []
  modified:
    - src/ct/cli.py
    - pyproject.toml
    - tests/test_cli.py
    - tests/test_manifest.py

key-decisions:
  - "pyyaml listed as runtime dependency (lowercase PyPI name, >=6.0 floor) — used by _species.py and manifest.py"
  - "species added to entry() passthrough set alongside other registered Typer subcommands"

patterns-established:
  - "entry() passthrough set must include every Typer subcommand registered with app.add_typer() — omissions silently route to natural-language query mode"

requirements-completed: [DATA-01, DATA-02, DATA-04]

duration: 8min
completed: 2026-02-26
---

# Phase 02.2 Plan 01: Integration Fixes II Summary

**"ag species" subcommand now routes through entry() correctly; PyYAML declared as runtime dep — both verified with targeted regression tests**

## Performance

- **Duration:** 8 min
- **Started:** 2026-02-26T09:30:00Z
- **Completed:** 2026-02-26T09:38:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `"species"` to `entry()` passthrough set in `src/ct/cli.py` — `ag species list` now dispatches to the Typer species subcommand instead of silently routing to the natural-language query handler
- Declared `pyyaml>=6.0` in `[project] dependencies` in `pyproject.toml` — previously used at runtime by `_species.py` and `manifest.py` but absent from install metadata
- Added `test_entry_preserves_species_subcommand` in `tests/test_cli.py` — verifies args routing without triggering real app invocation
- Added `test_pyyaml_declared_as_dependency` in `tests/test_manifest.py` — asserts pyyaml appears in pyproject.toml, prevents silent regression

## Task Commits

Each task was committed atomically:

1. **Task 1: Add species to entry() passthrough and declare PyYAML dependency** - `b1d04cc` (fix)
2. **Task 2: Add tests for species routing and PyYAML dependency declaration** - `eace3f9` (test)

## Files Created/Modified
- `src/ct/cli.py` — Added `"species"` to passthrough set in `entry()` (line 1467)
- `pyproject.toml` — Added `"pyyaml>=6.0"` to `[project] dependencies` (line 30)
- `tests/test_cli.py` — Added `test_entry_preserves_species_subcommand` after `test_entry_preserves_trace_subcommand`
- `tests/test_manifest.py` — Added `test_pyyaml_declared_as_dependency` at end of file

## Decisions Made
- PyPI package name `pyyaml` (lowercase) used — consistent with PyPI convention, distinguishable from the Python import name `yaml`
- `>=6.0` version floor rather than exact pin — allows compatible upgrades without forcing reinstall
- New species test placed immediately after trace subcommand test — groups all entry() routing tests together for discoverability

## Deviations from Plan

None — plan executed exactly as written. Both fixes were minimal one-line changes in the correct locations.

### Pre-existing Out-of-Scope Failures

Discovered and documented to `deferred-items.md`: 8 pre-existing failures in `tests/test_cli.py` caused by missing `ct.agent.trace` module and undefined `TraceLogger`. Confirmed pre-existing via `git stash` verification. Not caused by this plan's changes. Logged to `.planning/phases/02.2-integration-fixes-ii/deferred-items.md`.

## Issues Encountered

None — both fixes applied cleanly on first attempt.

## Next Phase Readiness

- `ag species list` routing is fully functional through the entry point
- PyYAML omission from install metadata is resolved — clean installs will no longer silently fail on `import yaml`
- Pre-existing `ct.agent.trace` failures should be addressed in a future integration fixes phase

---
*Phase: 02.2-integration-fixes-ii*
*Completed: 2026-02-26*
